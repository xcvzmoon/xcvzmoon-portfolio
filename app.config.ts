export default defineAppConfig({
  ui: {
    colors: {
      primary: 'red',
      neutral: 'zinc',
    },
    button: {
      defaultVariants: {
        size: 'lg',
      },
    },
    container: {
      base: 'max-w-[125rem] mx-auto px-4 sm:px-6 lg:px-8',
    },
  },
});
